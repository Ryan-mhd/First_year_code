#include<stdio.h>
void greeting(){
    printf("hello friend! ");
}

int main () {
greeting();
/*int a,b,d;

a = 6; //  00000110   
b = 10; // 00001010
d = 0; //  00001100
d = a ^ b; 

int *pA = &a;
printf("the address is: %p ", pA);
//printf("the result is: %d", d);

// xor = need one of them to be 1 and not both
// & = they need to be both 1
// | = at least one of them is 1
// << = move by one bit to the left
// >> = move by one bit to the right
// %p &a = address of the value
// *p = pointers to addresses, need to be the same data type ex: pApple
// * = depointerize, for ex: *pApple
// int apple;
// int *pApple = &apple;
// printf("%p", pApple);
// printf("depointerized: %d", *pApple);*/
return 0;
}